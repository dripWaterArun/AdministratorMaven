# 项目相关配置
project:
  #名称
  name: WenXi
  #作者
  author: daixu
  # 版本
  version: 1.0.0
  # 实例演示开关
  demoEnabled: true
  # 版权年份
  copyrightYear: 2021
  # 获取ip地址开关
  addressEnabled: false
  # 邮箱
  email: daixu_cisco@sina.cn

# 全局变量
variableBase:
  # Nacos 全局变量
  nacos:
    # 分组
    group:
      # 基础配置
      config: CONFIG_GROUP
      # 默认配置
      default: DEFAULT_GROUP
      # 数据库相关配置
      database: DATABASE_GROUP
    #地址
    addr:
      # 开发环境
      dev: 82.156.241.68:8848
      # 本地环境
      host: 82.156.241.68:8848
    #命名空间
    namespace:
      # 开发环境
      dev: 2a1214dd-09be-419a-8751-3f0be62b004b
      # 本地环境
      host: 2a1214dd-09be-419a-8751-3f0be62b004b

# Nacos 配置管理
configs:
  nacos:
    ################### 共享的配置文件 ###################
    # Nacos 配置文件 加载顺序
    #1、后加载的优先级高；
    #2、extension-configs 优先级大于 shared-configs，因为在 shared-configs 之后加载
    #3、shared-configs 与 extension-configs 的配置列表中，在下面的配置优先级高。
    shared:
      # Mybatis-plus 配置文件
      mybatis: mybatis-plus.yaml
      mybatis-group: ${variableBase.nacos.group.database}
      mybatis-refresh: true
      # druid 数据库连接池
      druid: druid.yaml
      druid-group: ${variableBase.nacos.group.database}
      druid-refresh: true
      # platform-demo-datasource 案例模块 数据库配置
      demo: platform-demo-datasource.yaml
      demo-group: ${variableBase.nacos.group.database}
      demo-refresh: true
      # platform-user-datasource 用户模块 数据库配置
      user: platform-user-datasource.yaml
      user-group: ${variableBase.nacos.group.database}
      user-refresh: true
    ################### 基础配置文件 ###################
    # 优先级大于 shared-configs，在 shared-configs 之后加载
    extension:
      # 基本配置
      base: nacos-config-base.yaml
      base-group: ${variableBase.nacos.group.config}
      base-refresh: true
      # 数据库基本配置
      database: nacos-config-database.yaml
      database-group: ${variableBase.nacos.group.config}
      database-refresh: true
      # 数据库共通配置
      druid: nacos-config-datasource.yaml
      druid-group: ${variableBase.nacos.group.config}
      druid-refresh: true
