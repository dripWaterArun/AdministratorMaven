# 项目相关配置
project:
  #名称
  name: WenXi
  # 版本
  version: 1.0.0
  # 版权年份
  copyrightYear: 2021

# Nacos 配置管理
nacos:
  configs:
    # 基础配置文件
    extension:
      base: nacos-config-base.yaml
      base-group: DEFAULT_GROUP
      base-refresh: true
    # 共享的配置文件
    shared:
      # Mybatis-plus 配置文件
      mybatis: mybatis-plus.yaml
      mybatis-group: DEFAULT_GROUP
      mybatis-refresh: true
